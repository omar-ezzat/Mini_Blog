apiVersion: v1
kind: Service
metadata:
  name: blog-service
  namespace: miniblog
spec:
  selector: { app: blog-service }
  ports: [{ port: 3002, targetPort: 3002 }]
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: blog-service
  namespace: miniblog
spec:
  replicas: 1
  selector: { matchLabels: { app: blog-service } }
  template:
    metadata: { labels: { app: blog-service } }
    spec:
      containers:
        - name: blog-service
          image: REGION-docker.pkg.dev/PROJECT_ID/REPOSITORY/blog-service:latest
          ports: [{ containerPort: 3002 }]
          env:
            - { name: PORT, value: "3002" }
            - name: MONGODB_URI
              valueFrom: { configMapKeyRef: { name: miniblog-config, key: BLOG_MONGODB_URI } }
            - name: JWT_SECRET
              valueFrom: { secretKeyRef: { name: miniblog-secrets, key: JWT_SECRET } }
            - name: JWT_EXPIRES_IN
              valueFrom: { configMapKeyRef: { name: miniblog-config, key: JWT_EXPIRES_IN } }
            - { name: KAFKA_CLIENT_ID, value: blog-service }
            - name: KAFKA_BROKERS
              valueFrom: { configMapKeyRef: { name: miniblog-config, key: KAFKA_BROKERS } }
            - name: KAFKA_TOPIC
              valueFrom: { configMapKeyRef: { name: miniblog-config, key: BLOG_KAFKA_TOPIC } }
          readinessProbe: { httpGet: { path: /health, port: 3002 }, initialDelaySeconds: 10, periodSeconds: 10 }
          livenessProbe: { httpGet: { path: /health, port: 3002 }, initialDelaySeconds: 30, periodSeconds: 20 }
