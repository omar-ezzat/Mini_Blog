# Replace before deployment. This intentionally contains no real credential.
apiVersion: v1
kind: Secret
metadata:
  name: miniblog-secrets
  namespace: miniblog
type: Opaque
stringData:
  JWT_SECRET: CHANGE_ME_TO_A_LONG_RANDOM_SECRET_BEFORE_DEPLOYING
